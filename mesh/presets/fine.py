# fine -- the reference mesh.  ~3.4 M cells per quadrant, ~2 min to build,
# 460 MB .msh per quadrant.  This is the parameter set meshParams.py
# documents; the preset exists so the name means something on the command line.
H_SCALE = 1.0
H_SCALE_WALL = True
